mutation login($identifier: String!, $password: String!) {
  login(input: { identifier: $identifier, password: $password }) {
    jwt
  }
}

query user($id: ID!) {
  user(id: $id) {
    id
    email
    firstName
    lastName
  }
}
